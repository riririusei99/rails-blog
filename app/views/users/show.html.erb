<div class="row"> 
  <div class="col s12 m12">
    <h4 class="header"><%= "#{@user.nickname}'s profile" %></h4>
    <%= render partial: "users/user", collection: [@user] %>
    <h4 class="header">Posted list</h4>
    <%= render partial: "posts/post", collection: @posts %>
    <%= paginate(@posts) %>
  </div>
</div>
